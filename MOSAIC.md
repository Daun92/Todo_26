# MOSAIC - 앱 정의서

> **"조각들이 모여 나만의 그림이 된다"**

---

## 1. 앱 개요

### 1.1 정체성
**Mosaic**은 개인 성장을 위한 AI 파트너 앱입니다.

넓고 다양한 관심사를 탐험하며, 각각의 지식 조각들이 어떻게 연결되는지 발견하고,
나만의 성장 알고리즘과 그 대척점을 함께 이해해 나가는 여정을 돕습니다.

### 1.2 핵심 철학
```
"넓고 얕게 탐험하며, 점들을 연결하고,
나만의 시각과 그 반대편을 동시에 이해하는 성장 파트너"
```

### 1.3 핵심 차별점

| 개념 | 의미 | 역할 |
|------|------|------|
| **나만의 알고리즘** | 성장 방향 및 경로 | 내가 어떻게 배우고 성장하는지 패턴 인식 |
| **대척점** | 기회 및 대안 | 편향을 인식하고 균형 잡힌 시각 제공 |

### 1.4 AI 파트너 역할
- 적극적으로 함께 성장하는 파트너
- 학습 중 질문하고 인사이트 발견 시 알림
- 일상적으로 대화하며 연결고리 발견
- 회고 시점을 제안하고 성장 스토리를 함께 작성

---

## 2. 사용자 프로필

### 2.1 관심 영역
```
┌─────────────────────────────────────────┐
│                                         │
│           철학/패러다임                  │
│                │                        │
│      정치 ─────┼───── 사회              │
│                │                        │
│           ┌────┴────┐                   │
│           │   Me    │                   │
│           └────┬────┘                   │
│                │                        │
│      신체 ─────┼───── 경제              │
│   (호흡,자세)  │    (주식,부동산)        │
│                │                        │
│               IT                        │
│                                         │
└─────────────────────────────────────────┘
```

### 2.2 학습 스타일
- **범위**: 넓고 얕게, 다양한 분야 탐험
- **목표**: 연결고리와 무의식 패턴 발견
- **방식**: AI 인터뷰 + 자유 메모 + 정리 학습

---

## 3. 핵심 기능 (4 Pillars)

### 3.1 FEED (발견) - Phase 1
> "오늘 뭘 배워볼까?"

#### 기능 목록
| 기능 | 설명 | 우선순위 |
|------|------|----------|
| AI 추천 피드 | 관심사 기반 학습 자료 추천 | P0 |
| 콘텐츠 풀 | 자동 수집된 콘텐츠 저장소 | P1 |
| 학습 큐 | 선택한 콘텐츠 학습 대기열 | P0 |
| 대척점 제안 | 반대 관점 자료 함께 제시 | P1 |

#### 데이터 소스
- RSS 피드 (뉴스, 블로그)
- 사용자 직접 입력 (URL, 텍스트)
- AI 추천 (키워드 기반)

#### UI 컴포넌트
- `FeedCard`: 추천 콘텐츠 카드
- `FeedList`: 피드 목록
- `LearningQueue`: 학습 큐
- `SourceManager`: 소스 관리

---

### 3.2 LEARN (학습/기록) - Phase 2
> "이걸 내 것으로 만들자"

#### 학습 모드
| 모드 | 설명 | 트리거 |
|------|------|--------|
| **인터뷰 모드** | AI가 질문 → 사용자 답변 | 콘텐츠 학습 시작 시 |
| **메모 모드** | 자유롭게 생각 기록 | 언제든지 |
| **정리 모드** | 메모 → 구조화 → 추가 학습 | 메모 누적 시 |

#### 인터뷰 질문 유형
```
1. 이해 확인: "이 내용에서 핵심은 뭐라고 생각해요?"
2. 연결 질문: "이전에 배운 것과 어떻게 연결되나요?"
3. 대척점 질문: "반대 의견은 어떻게 생각해요?"
4. 적용 질문: "이걸 어디에 적용할 수 있을까요?"
5. 감정 질문: "이 내용이 어떤 느낌을 주나요?"
```

#### UI 컴포넌트
- `InterviewSession`: 인터뷰 세션
- `MemoEditor`: 메모 에디터
- `MemoOrganizer`: 메모 정리
- `LearningProgress`: 학습 진행 상태

---

### 3.3 CONNECT (연결) - Phase 3
> "이것과 저것이 연결되네"

#### 기능 목록
| 기능 | 설명 |
|------|------|
| 지식 그래프 | 학습 내용의 연결 관계 시각화 |
| 태그 시스템 | 콘텐츠/메모에 태그 부여 |
| 무의식 패턴 | 반복되는 연결 패턴 발견 |
| 크로스 인사이트 | 다른 분야 간 연결점 발견 |

#### 시각화
```
[호흡법] ──── "집중력" ──── [투자 판단]
    │                          │
    └──── "스트레스" ──────────┘
              │
         [철학/마음]
```

#### UI 컴포넌트
- `KnowledgeGraph`: 지식 그래프 (D3.js)
- `TagCloud`: 태그 클라우드
- `ConnectionCard`: 연결 인사이트 카드
- `PatternAlert`: 패턴 발견 알림

---

### 3.4 REFLECT (회고) - Phase 4
> "이만큼 왔구나"

#### 회고 트리거
| 트리거 | 설명 |
|--------|------|
| 월간 정기 | 매월 말 자동 회고 프롬프트 |
| 분기 정기 | 분기별 종합 회고 |
| 온디맨드 | 사용자가 원할 때 |
| 이슈 발견 | AI가 특정 패턴/포인트 발견 시 제안 |

#### 회고 리포트 구성
```
📊 [기간] 성장 리포트

[학습량]
• 읽은 콘텐츠: N개
• 인터뷰 세션: N회
• 메모: N개

[발견한 연결고리]
• "A → B → C" 패턴 N회 언급

[나의 알고리즘]
• 선호 분야: ...
• 학습 패턴: ...

[대척점 균형]
• 현재 편향: ...
• 추천 탐험: ...

[작은 성공]
• 직접 경험한 성장 순간들
```

#### UI 컴포넌트
- `ReflectPrompt`: 회고 시작 프롬프트
- `ReflectReport`: 회고 리포트
- `GrowthTimeline`: 성장 타임라인
- `BiasBalance`: 편향 균형 시각화

---

## 4. 데이터 모델

### 4.1 핵심 엔티티

```typescript
// 콘텐츠 (학습 자료)
interface Content {
  id: string;
  type: 'article' | 'note' | 'url' | 'thought';
  title: string;
  source?: string;
  url?: string;
  body?: string;
  summary?: string;
  tags: string[];
  status: 'queued' | 'learning' | 'completed';
  counterpoint?: string; // 대척점
  createdAt: Date;
  completedAt?: Date;
}

// 인터뷰 세션
interface InterviewSession {
  id: string;
  contentId: string;
  exchanges: Exchange[];
  insights: string[];
  connections: Connection[];
  createdAt: Date;
}

interface Exchange {
  type: 'question' | 'answer';
  text: string;
  timestamp: Date;
}

// 메모
interface Memo {
  id: string;
  text: string;
  contentId?: string; // 연결된 콘텐츠
  sessionId?: string; // 연결된 인터뷰
  tags: string[];
  organized: boolean;
  createdAt: Date;
}

// 연결
interface Connection {
  id: string;
  sourceId: string;
  targetId: string;
  sourceType: 'content' | 'memo' | 'tag';
  targetType: 'content' | 'memo' | 'tag';
  relationship: string; // "관련", "대척점", "원인", "결과" 등
  strength: number; // 1-10
  createdAt: Date;
}

// 태그
interface Tag {
  id: string;
  name: string;
  category: 'topic' | 'emotion' | 'action' | 'custom';
  count: number;
}

// 회고
interface Reflection {
  id: string;
  type: 'monthly' | 'quarterly' | 'ondemand' | 'triggered';
  period: { start: Date; end: Date };
  report: ReflectionReport;
  createdAt: Date;
}

interface ReflectionReport {
  stats: LearningStats;
  patterns: Pattern[];
  algorithm: AlgorithmInsight;
  counterpoints: CounterpointBalance;
  successes: Success[];
  narrative?: string; // AI 생성 성장 스토리
}

// 사용자 프로필 (알고리즘 학습용)
interface UserProfile {
  interests: Interest[];
  learningPatterns: LearningPattern[];
  biases: Bias[];
  updatedAt: Date;
}
```

### 4.2 데이터베이스 테이블 (Dexie.js)

```typescript
class MosaicDB extends Dexie {
  contents!: Table<Content>;
  interviews!: Table<InterviewSession>;
  memos!: Table<Memo>;
  connections!: Table<Connection>;
  tags!: Table<Tag>;
  reflections!: Table<Reflection>;
  userProfile!: Table<UserProfile>;
}
```

---

## 5. 기술 스택

| 기술 | 버전 | 용도 |
|------|------|------|
| React | 19.x | UI 프레임워크 |
| TypeScript | 5.x | 타입 안전성 |
| Vite | 7.x | 빌드 도구 |
| Tailwind CSS | 4.x | 스타일링 |
| Zustand | 5.x | 상태 관리 |
| Dexie.js | 4.x | IndexedDB (오프라인) |
| D3.js | 7.x | 지식 그래프 시각화 |
| Recharts | 3.x | 차트/통계 |
| Lucide React | - | 아이콘 |

---

## 6. 프로젝트 구조

```
src/
├── components/
│   ├── feed/           # Feed 관련 컴포넌트
│   ├── learn/          # Learn 관련 컴포넌트
│   ├── connect/        # Connect 관련 컴포넌트
│   ├── reflect/        # Reflect 관련 컴포넌트
│   ├── layout/         # 레이아웃
│   └── ui/             # 공통 UI 컴포넌트
├── hooks/              # 커스텀 훅
├── lib/
│   ├── db.ts           # Dexie 데이터베이스
│   ├── ai.ts           # AI 서비스
│   └── utils.ts        # 유틸리티
├── pages/              # 페이지 컴포넌트
├── stores/             # Zustand 스토어
├── types/              # TypeScript 타입
├── App.tsx
├── main.tsx
└── index.css
```

---

## 7. MVP 로드맵

### Phase 1: Feed (발견)
- [ ] 콘텐츠 타입 정의 및 DB 설정
- [ ] 콘텐츠 추가 (URL, 텍스트)
- [ ] 학습 큐 관리
- [ ] 기본 피드 UI

### Phase 2: Learn (학습)
- [ ] 인터뷰 세션 구현
- [ ] 메모 시스템
- [ ] 학습 완료 처리
- [ ] 태그 시스템

### Phase 3: Connect (연결)
- [ ] 연결 데이터 모델
- [ ] 기본 지식 그래프
- [ ] 태그 기반 연결
- [ ] 패턴 발견 알고리즘

### Phase 4: Reflect (회고)
- [ ] 회고 리포트 생성
- [ ] 성장 타임라인
- [ ] 편향 분석
- [ ] 성장 스토리 생성

---

## 8. UI/UX 가이드

### 8.1 디자인 원칙
- **미니멀**: 핵심에 집중, 불필요한 요소 제거
- **대화형**: AI와의 자연스러운 대화 경험
- **시각적 피드백**: 성장과 연결을 눈으로 확인

### 8.2 네비게이션
```
┌─────────────────────────────────────┐
│  MOSAIC                    [설정]   │
├─────────────────────────────────────┤
│                                     │
│           [현재 페이지]              │
│                                     │
├─────────────────────────────────────┤
│  [Feed]  [Learn]  [Connect] [Reflect]│
└─────────────────────────────────────┘
```

### 8.3 색상 시스템
```css
/* Primary - 성장/연결 */
--primary: #6366f1; /* Indigo */

/* Secondary - 대척점/균형 */
--secondary: #f59e0b; /* Amber */

/* Neutral */
--background: #ffffff;
--foreground: #0f172a;

/* Dark Mode */
--background-dark: #0f172a;
--foreground-dark: #f8fafc;
```

---

## 9. 향후 확장

### Phase 5+
- [ ] PWA 지원
- [ ] 클라우드 동기화
- [ ] AI 고도화 (Claude API 연동)
- [ ] 알림 시스템
- [ ] 데이터 내보내기/가져오기
- [ ] 커뮤니티 기능

---

*Last Updated: 2025-12-21*
