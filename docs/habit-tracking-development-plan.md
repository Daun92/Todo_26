# 습관 관리 기능 개발 계획

## 개요

습관 관리 탭을 완성하고, 사용자가 꾸준히 습관을 유지할 수 있도록 동기부여하는 UX 장치를 구현합니다.

---

## 현재 구현 상태

### ✅ 완료된 기능
- 일일 습관 토글 (체크/해제)
- 연속 기록(스트릭) 표시
- GitHub 스타일 히트맵 (연간 활동)
- 기본 습관 5개 (걷기, 자세, 코어, 호흡, 명상)
- 다크모드 지원

### ❌ 미구현 기능
- 습관 추가/수정/삭제 UI
- 에너지/기분 기록 UI
- 동기부여 시스템 (배지, 레벨, 성취)
- 습관별 개별 통계
- 독립적인 습관 탭/페이지

---

## 개발 계획

### Phase 1: 핵심 습관 관리 기능

#### 1.1 습관 설정 모달 (`HabitSettingsModal.tsx`)
```
구현 내용:
├── 습관 목록 표시 (드래그 순서 변경)
├── 습관 추가 폼
│   ├── 이름 입력
│   ├── 아이콘 선택 (이모지 피커)
│   └── 설명 입력 (선택)
├── 습관 수정/삭제
└── 습관 활성화/비활성화 토글
```

**파일 생성:**
- `src/components/habits/HabitSettingsModal.tsx`
- `src/components/habits/HabitForm.tsx`
- `src/components/habits/EmojiPicker.tsx`

**Hook 수정:**
- `src/hooks/useHabits.ts` - CRUD 함수 추가
  - `addHabit(habit: Partial<HabitDefinition>)`
  - `updateHabit(id: string, updates: Partial<HabitDefinition>)`
  - `deleteHabit(id: string)`
  - `reorderHabits(ids: string[])`

#### 1.2 독립 습관 페이지 (`HabitsPage.tsx`)
```
레이아웃:
┌─────────────────────────────────────┐
│  오늘의 습관           [설정 ⚙️]   │
├─────────────────────────────────────┤
│  ┌──────┐ ┌──────┐ ┌──────┐        │
│  │ 🚶  │ │ 📐  │ │ 💪  │ ...    │
│  │ 걷기 │ │ 자세 │ │ 코어 │        │
│  └──────┘ └──────┘ └──────┘        │
├─────────────────────────────────────┤
│  📊 진행률 ████████░░ 60% (3/5)    │
├─────────────────────────────────────┤
│  🔥 연속 기록                       │
│  ┌────────────────────────────────┐ │
│  │  현재 스트릭: 7일              │ │
│  │  최고 기록: 23일               │ │
│  │  이번 달 완료율: 85%           │ │
│  └────────────────────────────────┘ │
├─────────────────────────────────────┤
│  📅 주간 히트맵                     │
│  월 화 수 목 금 토 일              │
│  ██ ██ ░░ ██ ██ ░░ ░░              │
├─────────────────────────────────────┤
│  😊 오늘의 컨디션 (선택)           │
│  에너지: ○ ○ ○ ● ○                │
│  기분:   😔 😐 😊 😄 🤩            │
│  메모:   ___________________       │
└─────────────────────────────────────┘
```

**파일 생성:**
- `src/pages/HabitsPage.tsx`
- `src/components/habits/HabitGrid.tsx`
- `src/components/habits/WeeklyMiniHeatmap.tsx`
- `src/components/habits/ConditionTracker.tsx`

---

### Phase 2: 동기부여 UX 시스템

#### 2.1 습관 완료 피드백 강화

**즉각적 피드백 (마이크로 인터랙션)**
```typescript
// 습관 완료 시 시각적 효과
- 체크 애니메이션 (스케일 + 바운스)
- 색상 전환 (회색 → 초록 그라데이션)
- 아이콘 파티클 효과 (완료 순간)
- 진동 피드백 (모바일, navigator.vibrate)
```

**일일 목표 달성 축하**
```
구현 내용:
├── 50% 달성: 격려 토스트 ("반이나 했어요! 💪")
├── 100% 달성: 축하 모달 + 컨페티 효과
└── 스트릭 마일스톤 (7일, 30일, 100일): 배지 획득
```

**파일 생성:**
- `src/components/habits/CompletionCelebration.tsx`
- `src/components/ui/Confetti.tsx`
- `src/components/ui/Toast.tsx`

#### 2.2 배지/성취 시스템 (`AchievementSystem`)

**배지 카테고리**
```
🌱 시작 배지
├── 첫 습관 완료 "첫 걸음"
├── 첫 전체 완료 "완벽한 하루"
└── 첫 주 연속 "일주일 챔피언"

🔥 스트릭 배지
├── 7일 연속 "불꽃 시작"
├── 30일 연속 "습관 마스터"
├── 100일 연속 "전설의 시작"
└── 365일 연속 "1년의 기적"

📊 통계 배지
├── 100회 습관 완료 "백번의 노력"
├── 월간 90%+ 달성 "이달의 챔피언"
└── 3개월 연속 80%+ "꾸준함의 증명"

🌟 특별 배지
├── 주말에도 완료 "주말 전사"
├── 아침 6시 전 완료 "얼리버드"
└── 모든 습관 30일 유지 "균형 잡힌 삶"
```

**데이터 모델 추가** (`src/types/index.ts`)
```typescript
interface Achievement {
  id: string;
  type: 'streak' | 'milestone' | 'special';
  name: string;
  description: string;
  icon: string;
  condition: AchievementCondition;
  unlockedAt?: string; // ISO date
}

interface UserAchievements {
  id: string;
  achievements: string[]; // achievement IDs
  totalPoints: number;
}
```

**파일 생성:**
- `src/components/habits/AchievementBadge.tsx`
- `src/components/habits/AchievementModal.tsx`
- `src/components/habits/AchievementList.tsx`
- `src/hooks/useAchievements.ts`
- `src/lib/achievements.ts` (배지 정의 및 조건 체크)

#### 2.3 스트릭 보호 시스템

**스트릭 프리즈 (Freeze)**
```
기능:
├── 월 1-2회 스트릭 프리즈 사용 가능
├── 바쁜 날 미리 설정하여 스트릭 유지
├── 사용 시 히트맵에 ❄️ 표시
└── 프리즈 사용 횟수 표시
```

**스트릭 복구 (Recovery)**
```
기능:
├── 하루 놓친 경우, 다음 날 2배 완료로 복구
├── 복구 기회는 월 1회 제한
└── 복구 성공 시 배지 획득
```

**파일 생성:**
- `src/components/habits/StreakProtection.tsx`
- `src/hooks/useStreakProtection.ts`

#### 2.4 격려 메시지 시스템

**상황별 메시지**
```typescript
const encouragements = {
  morning: [
    "좋은 아침이에요! 오늘도 작은 습관부터 시작해볼까요? ☀️",
    "새로운 하루, 새로운 기회! 첫 습관을 체크해보세요 🌱"
  ],
  halfComplete: [
    "벌써 절반이나! 이 기세로 오늘도 완주해봐요 💪",
    "반이나 끝났어요! 남은 것도 금방이에요 🎯"
  ],
  almostDone: [
    "마지막 하나! 오늘의 완벽한 하루를 만들어봐요 ✨",
    "거의 다 왔어요! 피니시 라인이 눈앞이에요 🏃"
  ],
  streakWarning: [
    "🔥 스트릭이 위험해요! 오늘 습관을 완료해주세요",
    "연속 기록 유지까지 습관 하나만 더! 🎖️"
  ],
  comeback: [
    "다시 돌아오셨네요! 언제든 새로 시작할 수 있어요 🌈",
    "오랜만이에요! 오늘부터 다시 함께해요 💪"
  ]
}
```

**표시 위치:**
- 습관 페이지 상단 배너
- 푸시 알림 (Phase 3)
- 홈 위젯

---

### Phase 3: 고급 통계 및 인사이트

#### 3.1 습관별 개별 통계

**HabitDetailModal.tsx**
```
┌─────────────────────────────────────┐
│  🚶 걷기                    [수정]  │
├─────────────────────────────────────┤
│  📊 통계                            │
│  ├── 총 완료: 127회                 │
│  ├── 이번 달: 23/26일 (88%)        │
│  ├── 현재 스트릭: 12일             │
│  └── 최고 스트릭: 34일             │
├─────────────────────────────────────┤
│  📈 주간 트렌드                     │
│  [완료율 라인 차트 - 최근 8주]      │
├─────────────────────────────────────┤
│  📅 완료 요일 분포                  │
│  월████ 화████ 수███ 목████       │
│  금██░░ 토░░░░ 일░░░░              │
├─────────────────────────────────────┤
│  💡 인사이트                        │
│  "평일에 더 잘 하시네요!            │
│   주말 루틴을 만들어보는 건 어때요?" │
└─────────────────────────────────────┘
```

**파일 생성:**
- `src/components/habits/HabitDetailModal.tsx`
- `src/components/habits/HabitTrendChart.tsx`
- `src/components/habits/DayDistributionChart.tsx`
- `src/hooks/useHabitStats.ts`

#### 3.2 습관 연관성 분석

**상관관계 시각화**
```
- 습관 A 완료 시 습관 B 완료율 변화
- 에너지/기분과 습관 완료율 상관관계
- 요일별 완료 패턴 분석
```

#### 3.3 AI 코치 연동

**인사이트 생성**
```
- 완료율 기반 격려/조언
- 패턴 분석 (언제 가장 잘 하는지)
- 개선 제안 (놓치기 쉬운 습관 강조)
```

---

## 기술 구현 상세

### 데이터베이스 스키마 확장

```typescript
// src/types/index.ts 추가

interface HabitDefinition {
  // 기존 필드
  id: string;
  name: string;
  icon: string;
  description?: string;
  active: boolean;
  order: number;

  // 신규 필드
  color?: string;           // 커스텀 색상
  frequency?: 'daily' | 'weekdays' | 'weekends' | 'custom';
  customDays?: number[];    // 0-6 (일-토)
  reminder?: string;        // HH:mm 형식
  createdAt: string;
  archivedAt?: string;
}

interface HabitLog {
  // 기존 필드
  id: string;
  date: string;
  habits: Record<string, boolean>;

  // 신규 필드
  energy?: 1 | 2 | 3 | 4 | 5;
  mood?: 1 | 2 | 3 | 4 | 5;
  note?: string;
  completedAt?: Record<string, string>; // habitId -> ISO timestamp
}

interface Achievement {
  id: string;
  type: 'streak' | 'milestone' | 'special';
  name: string;
  nameKo: string;
  description: string;
  descriptionKo: string;
  icon: string;
  points: number;
  condition: {
    type: 'streak' | 'total' | 'rate' | 'special';
    value: number;
    period?: 'day' | 'week' | 'month';
  };
}

interface UserStats {
  id: string;
  unlockedAchievements: Array<{
    achievementId: string;
    unlockedAt: string;
  }>;
  totalPoints: number;
  streakFreezes: {
    remaining: number;
    usedDates: string[];
  };
  streakRecovery: {
    available: boolean;
    lastUsed?: string;
  };
}
```

### 네비게이션 구조

```typescript
// BottomNav 탭 구조
const tabs = [
  { id: 'home', icon: Home, label: '홈' },
  { id: 'habits', icon: CheckSquare, label: '습관' },  // 신규
  { id: 'goals', icon: Target, label: '목표' },
  { id: 'insights', icon: TrendingUp, label: '분석' },
  { id: 'journal', icon: BookOpen, label: '저널' },
];
```

### 컴포넌트 트리

```
src/
├── components/
│   ├── habits/
│   │   ├── index.ts
│   │   ├── HabitGrid.tsx              # 습관 그리드 (메인)
│   │   ├── HabitButton.tsx            # 개별 습관 버튼
│   │   ├── HabitSettingsModal.tsx     # 습관 관리 모달
│   │   ├── HabitForm.tsx              # 습관 추가/수정 폼
│   │   ├── HabitDetailModal.tsx       # 습관 상세 통계
│   │   ├── EmojiPicker.tsx            # 이모지 선택기
│   │   ├── WeeklyMiniHeatmap.tsx      # 주간 미니 히트맵
│   │   ├── ConditionTracker.tsx       # 에너지/기분 입력
│   │   ├── StreakDisplay.tsx          # 스트릭 표시 (확장)
│   │   ├── StreakProtection.tsx       # 스트릭 보호 UI
│   │   ├── AchievementBadge.tsx       # 배지 컴포넌트
│   │   ├── AchievementList.tsx        # 배지 목록
│   │   ├── AchievementModal.tsx       # 배지 획득 모달
│   │   ├── CompletionCelebration.tsx  # 완료 축하 효과
│   │   ├── EncouragementBanner.tsx    # 격려 메시지
│   │   ├── HabitTrendChart.tsx        # 트렌드 차트
│   │   └── DayDistributionChart.tsx   # 요일 분포 차트
│   └── ui/
│       ├── Confetti.tsx               # 컨페티 효과
│       └── Toast.tsx                  # 토스트 알림
├── hooks/
│   ├── useHabits.ts                   # 수정 (CRUD 추가)
│   ├── useHabitStats.ts               # 신규 (통계)
│   ├── useAchievements.ts             # 신규 (성취)
│   └── useStreakProtection.ts         # 신규 (스트릭 보호)
├── lib/
│   ├── achievements.ts                # 배지 정의
│   └── encouragements.ts              # 격려 메시지
└── pages/
    └── HabitsPage.tsx                 # 신규 습관 페이지
```

---

## 개발 우선순위

### 🔴 P0 - 핵심 (1주차)
1. 독립 습관 페이지 생성 (`HabitsPage.tsx`)
2. 습관 관리 모달 (추가/수정/삭제)
3. 에너지/기분 입력 UI
4. BottomNav에 습관 탭 추가

### 🟠 P1 - 동기부여 (2주차)
5. 습관 완료 애니메이션 강화
6. 일일 완료 축하 모달 + 컨페티
7. 기본 배지 시스템 (5-10개)
8. 격려 메시지 배너

### 🟡 P2 - 통계 (3주차)
9. 습관별 상세 통계 모달
10. 주간 트렌드 차트
11. 요일 분포 분석
12. 최고 스트릭 기록

### 🟢 P3 - 고급 (4주차+)
13. 스트릭 보호 시스템
14. AI 인사이트 연동
15. 습관-목표 연결
16. 알림 시스템

---

## UX 설계 원칙

### 1. 즉각적 피드백
- 모든 인터랙션에 0.2초 이내 시각적 반응
- 햅틱 피드백 (모바일)
- 상태 변화 애니메이션

### 2. 작은 승리 축하
- 50%, 100% 달성 시 축하
- 스트릭 마일스톤 강조
- 배지 획득 즉시 알림

### 3. 부정적 피드백 최소화
- "실패" 대신 "다시 시작"
- 스트릭 끊김 시 "새로운 도전의 시작"
- 과거 기록은 항상 긍정적으로 표현

### 4. 점진적 난이도
- 처음엔 쉬운 배지부터
- 장기 목표는 중간 마일스톤 제공
- 무리한 목표 설정 방지

### 5. 개인화
- 사용자 패턴에 맞는 격려
- 시간대별 맞춤 메시지
- 선호 습관 강조

---

## 예상 결과물

### 습관 페이지 최종 모습

```
┌─────────────────────────────────────┐
│  🌅 좋은 아침이에요!                │
│  오늘도 작은 습관부터 시작해봐요    │
├─────────────────────────────────────┤
│  오늘의 습관              ⚙️ 설정  │
│                                     │
│  ┌──────┐ ┌──────┐ ┌──────┐       │
│  │ 🚶 ✓│ │ 📐  │ │ 💪 ✓│ ...   │
│  │ 걷기 │ │ 자세 │ │ 코어 │       │
│  └──────┘ └──────┘ └──────┘       │
│                                     │
│  ████████████░░░░ 60% (3/5)        │
├─────────────────────────────────────┤
│  🔥 7일 연속    🏆 최고: 23일      │
├─────────────────────────────────────┤
│  이번 주                            │
│  월 화 수 목 금 토 일              │
│  ✓  ✓  ○  ✓  •  •  •              │
├─────────────────────────────────────┤
│  오늘 컨디션 (선택)                 │
│  에너지: ⚡⚡⚡⚡○                   │
│  기분:   😊                         │
├─────────────────────────────────────┤
│  🎖️ 최근 획득 배지                 │
│  [🌱 첫 걸음] [🔥 일주일 챔피언]   │
└─────────────────────────────────────┘
```

---

## 다음 단계

1. ✅ 개발 계획 승인 받기
2. ⬜ P0 기능 구현 시작
3. ⬜ 디자인 시스템 컴포넌트 생성
4. ⬜ 단계별 테스트 및 피드백

---

*마지막 업데이트: 2025-12-17*
